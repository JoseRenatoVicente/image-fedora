# Hide the GRUB menu and boot immediately.
# Overrides the 1-second timeout in grub-static-pre.cfg.
# The user can still reach the GRUB menu by holding Shift/Escape at power-on.
set timeout_style=hidden
set timeout=0
