[Journal]
SystemMaxUse=500M
SystemMaxFileSize=50M
# Permite a membros do grupo wheel ler o journal do sistema sem sudo.
# Ubuntu adiciona utilizadores ao grupo 'adm' para o mesmo efeito;
# em Fedora o equivalente é 'wheel' (administradores locais).
SystemGroup=wheel systemd-journal
