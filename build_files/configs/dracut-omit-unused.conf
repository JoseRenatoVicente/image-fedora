# Omitir módulos desnecessários para hardware Intel/Dell — reduz tamanho do initramfs
# mdraid: sem RAID por software; cifs/nfs: sem rootfs em rede
omit_dracutmodules+=" mdraid cifs nfs "
# Compressão zstd: mais rápida que xz com ratio semelhante
compress="zstd"
