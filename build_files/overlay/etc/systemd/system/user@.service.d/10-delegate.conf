[Service]
# Delega controladores de cgroup às sessões de utilizador, permitindo que
# podman/distrobox rootless e systemd-run façam resource control (cpu, io,
# memória) sem privilégios. Derivado de cachyos-settings.
Delegate=cpu cpuset io memory pids
