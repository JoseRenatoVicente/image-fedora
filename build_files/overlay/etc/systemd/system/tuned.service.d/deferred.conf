[Unit]
# Remove After=network.target do tuned upstream.
# O perfil balanced configura CPU/energia e nao precisa de rede activa.
# After= vazio limpa todos os After= acumulados; os seguintes redefinem
# apenas as dependencias minimas necessarias.
After=
After=systemd-sysctl.service dbus.service polkit.service
