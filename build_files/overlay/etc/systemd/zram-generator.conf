# Swap comprimido em RAM. Limitado a 4 GB ou 1/4 da RAM para evitar
# pressão excessiva em máquinas com muita memória.
[zram0]
zram-size = min(ram / 4, 4096)
compression-algorithm=zstd
swap-priority = 100
fs-type = swap
