# Swap comprimido em RAM para sistemas com pouca memória.
# zram-size = ram / 2 é conservador para hosts com 4–8 GB.
[zram0]
zram-size = ram / 2
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
