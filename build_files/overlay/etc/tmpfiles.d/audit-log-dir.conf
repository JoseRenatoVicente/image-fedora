# Garante /var/log/audit no primeiro boot (modelo atómico: /var é mutável e
# configurado por tmpfiles, nunca baked). Sem o diretório o auditd.service falha
# a arrancar. 0700 root:root — logs de auditoria não são world-readable.
d /var/log/audit 0700 root root - -
