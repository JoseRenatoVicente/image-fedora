[Daemon]
AutomaticUpdatePolicy=stage
# Don't pull in "Recommended" packages on updates — minimises attack surface.
# Recommended packages are opt-in extras, not hard dependencies; excluding them
# prevents transitive packages from silently expanding the installed base.
Recommends=false
