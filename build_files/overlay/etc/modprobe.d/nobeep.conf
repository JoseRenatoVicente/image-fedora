# Anti-incômodo (não é hardening): silencia o beep do PC speaker em erros
# de terminal. blacklist basta — só impede auto-load, que é o único caminho
# de carga; nada faz modprobe destes manualmente. Derivado de Garuda.
blacklist pcspkr
blacklist snd-pcsp
