# Force dnf5 COPR chroot detection to use "fedora" as the distribution,
# regardless of the custom VARIANT_ID set in os-release.
# Without this, dnf5 looks for a chroot named after our VARIANT_ID
# (e.g. "fedora-kde-custom-44-x86_64") which doesn't exist in COPR.
[main]
distribution = fedora
